<view class="page">
  <view class="card">
    <text class="title">用户档案</text>
    <text class="muted block">迁移 H5「档案 + 引导问卷」：写入 wx_user_profile（JSON）。</text>
    <text class="preview muted" wx:if="{{preview}}">{{preview}}</text>
    <button class="btn-primary full" bindtap="onClear">清空本地数据（同 H5）</button>
  </view>
</view>
